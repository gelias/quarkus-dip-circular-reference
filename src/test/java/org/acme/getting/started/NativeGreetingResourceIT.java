package org.acme.getting.started;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends VersionResourceTest {

    // Execute the same tests but in native mode.
}